% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_city.R
\name{create_city}
\alias{create_city}
\title{Create a 3D city array}
\usage{
create_city(
  zone_dim = c(2, 2),
  lot_dim = c(3, 3),
  NA_zone_ind = NULL,
  NA_lot_ind = NULL,
  max_height = 2,
  height_prop = rep(1, max_height)/max_height
)
}
\arguments{
\item{zone_dim}{Integer vector of length 2: number of zones (rows, cols).}

\item{lot_dim}{Integer vector of length 2: lot dimensions within each zone.}

\item{NA_zone_ind}{Integer vector of zone indices to set as NA. Default NULL.}

\item{NA_lot_ind}{Integer vector of lot indices (within a zone) to set as NA. Default NULL.}

\item{max_height}{Positive integer: maximum building height.}

\item{height_prop}{Numeric vector of length \code{max_height} giving the
proportion of buildings at each height. Will be normalized to sum to 1.}
}
\value{
A 3D array of dimension \code{c(zone_dim[1]*lot_dim[1], zone_dim[2]*lot_dim[2], max_height)}.
The first two axes are spatial coordinates, the third axis is height level.
Values are zone numbers; cells above a building's height or in NA zones/lots are NA.
}
\description{
Generates a 3D array representing a city composed of zones and lots,
with randomly assigned building heights.
}
\note{
Requires the external function \code{make_group_labels(n, prop, random_adjustment)}.
}
