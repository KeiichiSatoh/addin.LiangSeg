% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_landlord.R
\name{create_landlord}
\alias{create_landlord}
\title{Create landlords and assign property ownership in a city}
\usage{
create_landlord(
  city,
  n_landlord = 5,
  minority_aversion = c(1, 1, 1, 0, 0),
  SES_aversion = c(0, 0, 1, 1, 1),
  intercept_aversion = rep(1, n_landlord),
  dispersed_landlord = c(1, 1, 1, 0, 0),
  geo_difference = FALSE,
  geo_lambda = 1,
  landlord_origin_vec = NULL
)
}
\arguments{
\item{city}{A 3D array representing a city, typically produced by
\code{\link[=create_city]{create_city()}}. The first two dimensions are spatial coordinates and
the third is the height axis. NA cells are treated as non-housing and
receive no owner.}

\item{n_landlord}{A positive integer giving the number of landlords to
create. Default is \code{5}.}

\item{minority_aversion}{A non-negative numeric vector of length
\code{n_landlord} representing each landlord's aversion to ethnic
minority. Higher values indicate stronger aversion.
Default is \code{c(1, 1, 1, 0, 0)}.}

\item{SES_aversion}{A non-negative numeric vector of length
\code{n_landlord} representing each landlord's aversion based on
socioeconomic status (SES). The interpretation is application-
specific; higher values typically indicate stronger aversion.
Default is \code{c(0, 0, 1, 1, 1)}.}

\item{dispersed_landlord}{An integer vector of length \code{n_landlord}
containing 0 or 1. A value of \code{1} marks the landlord as "dispersed",
meaning they own property uniformly across the city (rather than
clustered near an origin point). Only relevant when
\code{geo_difference = TRUE}.}

\item{geo_difference}{Logical. If \code{TRUE}, ownership is assigned based
on geographic distance from each landlord's origin, with closer cells
being more likely to be owned by that landlord. If \code{FALSE}, ownership
is assigned uniformly at random. Default is \code{FALSE}.}

\item{geo_lambda}{A positive numeric scalar controlling the rate of
distance decay in the ownership propensity. Higher values produce
more spatially clustered ownership. Default is \code{1}. Only used when
\code{geo_difference = TRUE}.}

\item{landlord_origin_vec}{An optional integer vector of length
\code{n_landlord} giving the cell index (in column-major order over the
first two dimensions of \code{city}) of each landlord's origin point.
Must contain unique values within \code{1:prod(dim(city)[1:2])}. If \code{NULL}
(the default), origins are sampled at random without replacement.
Only used when \code{geo_difference = TRUE}.}

\item{aversion_intercept}{A intercept for calculating the aversion score.}
}
\value{
A list with two elements:
\describe{
\item{\code{landlord}}{A data frame with columns \code{ID}, \code{minority_aversion},
\code{SES}, and \code{dispersed}, one row per landlord.}
\item{\code{ownership}}{A 3D integer array with the same dimensions as
\code{city}. Each non-NA cell contains the ID of the landlord that
owns it; cells that are NA in \code{city} are also NA here. Ownership
is constant along the height axis (a building has a single owner
across all of its levels).}
}
}
\description{
Generates a set of landlords with specified aversion parameters and
assigns ownership of each cell in a city array. Ownership can be
assigned either uniformly at random or based on geographic distance
from each landlord's origin point, with support for "dispersed"
landlords who own property uniformly across the city.
}
\details{
When \code{geo_difference = TRUE}, ownership propensity for each landlord is
computed as \code{exp(-geo_lambda * d)}, where \code{d} is the Euclidean distance
from the landlord's origin to the target cell. For each cell, the owner
is sampled with probability proportional to these propensities across
landlords. Cells that coincide with a landlord's origin are assigned
deterministically to that landlord.

Dispersed landlords (those with \code{dispersed_landlord == 1}) have their
propensity row replaced by its row mean, yielding a uniform propensity
across all cells while preserving the same scale as non-dispersed
landlords. This makes them compete on equal footing across the entire
city rather than clustering near an origin.
}
\examples{
\dontrun{
city <- create_city(zone_dim = c(2, 2), lot_dim = c(3, 3), max_height = 2)

# Uniform random ownership
result1 <- create_landlord(city, n_landlord = 3,
                           minority_aversion = c(1, 0, 0),
                           SES    = c(0, 1, 0),
                           dispersed_landlord = c(0, 0, 0))

# Geographically clustered ownership with two dispersed landlords
result2 <- create_landlord(city, n_landlord = 5,
                           minority_aversion = c(1, 1, 1, 0, 0),
                           SES_aversion    = c(0, 0, 1, 1, 1),
                           dispersed_landlord = c(0, 0, 0, 1, 1),
                           geo_difference = TRUE,
                           geo_lambda = 0.5)
}

}
\seealso{
\code{\link[=create_city]{create_city()}}, \code{\link[=create_resident]{create_resident()}}, \code{\link[=get_n_resident]{get_n_resident()}}
}
