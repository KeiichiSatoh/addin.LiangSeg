% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_n_resident.R
\name{get_n_resident}
\alias{get_n_resident}
\title{Calculate the number of residents in a city}
\usage{
get_n_resident(city, vacant_prop)
}
\arguments{
\item{city}{An array representing a city, typically produced by
\code{\link[=create_city]{create_city()}}. Non-NA cells are treated as housing units; NA cells
are excluded from the count.}

\item{vacant_prop}{A single numeric value in \verb{[0, 1]} giving the proportion
of houses that are vacant. The remaining proportion \code{(1 - vacant_prop)}
is occupied by residents.}
}
\value{
An integer scalar: the number of residents (i.e. occupied houses),
computed as \code{round(n_houses * (1 - vacant_prop))}.
}
\description{
Computes the number of occupied housing units in a city, given a vacancy
proportion. The total number of houses is determined by counting non-NA
cells in the city array.
}
\details{
The function counts every non-NA cell in \code{city} as a distinct house.
For a 3D city array (with a height axis), this means each occupied
level of a building counts as one house.
}
\examples{
\dontrun{
city <- create_city(zone_dim = c(2, 2), lot_dim = c(3, 3), max_height = 2)
get_n_resident(city, vacant_prop = 0.1)
}

}
\seealso{
\code{\link[=create_city]{create_city()}}
}
